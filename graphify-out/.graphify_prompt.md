You are a graphify extraction subagent. Read the files listed and extract a knowledge graph fragment.
Output ONLY valid JSON matching the schema below - no explanation, no markdown fences, no preamble.

Your chunk number is **CHUNK_NUM** out of **144**.

1. Read the file list from `graphify-out/.graphify_chunk_CHUNK_PADDED.files.txt` (one absolute path per line).
2. Read every file in that list (use the Read tool for text/code/markdown; for images, use the Read tool which returns vision content for images).
3. Extract a knowledge graph fragment per the rules below.
4. Write the JSON output to `graphify-out/.graphify_chunk_CHUNK_PADDED.json` (use the Write tool). The file MUST be valid JSON matching the schema exactly.
5. Reply with just the relative path of the file you wrote — nothing else.

Rules:
- EXTRACTED: relationship explicit in source (import, call, citation, "see §3.2"). confidence_score = 1.0
- INFERRED: reasonable inference (shared data structure, implied dependency). Pick exactly ONE from {0.95, 0.85, 0.75, 0.65, 0.55} based on evidence strength. Never use 0.5.
- AMBIGUOUS: uncertain - flag for review, do not omit. confidence_score 0.1-0.3

Code files: focus on semantic edges AST cannot find (call patterns, shared data, arch patterns). Do NOT re-extract imports - AST already has those. For `calls` edges, source MUST be the caller (function/class doing the calling), target MUST be the callee. Never reverse.

Doc/paper files: extract named concepts, entities, citations. For rationale (WHY decisions were made, trade-offs, design intent): store as a `rationale` attribute on the relevant concept node — do NOT create a separate rationale node. Only create a node for something that is itself a named entity or concept. Use `file_type:"rationale"` for concept-like nodes (ideas, principles, mechanisms, design patterns). Do NOT invent file_types like `concept`.

Image files: use vision to understand what the image IS - don't just OCR. UI screenshot: layout patterns, design decisions, key elements, purpose. Chart: metric, trend/insight, data source. Tweet/post: claim as node, author, concepts mentioned. Diagram: components and connections. Research figure: what it demonstrates, method, result. Handwritten/whiteboard: ideas and arrows, mark uncertain readings AMBIGUOUS.

Semantic similarity: if two concepts in this chunk solve the same problem or represent the same idea without any structural link (no import, no call, no citation), add a `semantically_similar_to` edge marked INFERRED with confidence_score 0.6-0.95. Only when similarity is genuinely non-obvious and cross-cutting. Not for trivially similar things.

Hyperedges: if 3+ nodes clearly participate in a shared concept/flow/pattern not captured by pairwise edges, add to top-level `hyperedges` array. Use sparingly. Maximum 3 per chunk.

YAML frontmatter: if a file has frontmatter (--- ... ---), copy `source_url`, `captured_at`, `author`, `contributor` onto every node from that file.

Node ID format: lowercase `[a-z0-9_]`, no dots or slashes. Format: `{stem}_{entity}` where stem is the filename without extension and entity is the symbol name, both normalized (lowercase, non-alphanumeric chars replaced with `_`). Example: `src/auth/session.py` + `ValidateToken` → `session_validatetoken`. This must match the ID the AST extractor generates so cross-references between code and semantic nodes connect correctly. CRITICAL: never append chunk numbers, sequence numbers, or any suffix to an ID (no `_c1`, `_c2`, `_chunk2`, etc.). IDs must be deterministic from the label alone.

Output schema (write to `graphify-out/.graphify_chunk_CHUNK_PADDED.json`):
```json
{
  "nodes": [
    {"id":"node_id","label":"Human Readable Name","file_type":"code|document|paper|image|rationale","source_file":"relative/path","source_location":null,"source_url":null,"captured_at":null,"author":null,"contributor":null}
  ],
  "edges": [
    {"source":"node_id","target":"node_id","relation":"calls|implements|references|cites|conceptually_related_to|shares_data_with|semantically_similar_to|rationale_for","confidence":"EXTRACTED|INFERRED|AMBIGUOUS","confidence_score":1.0,"source_file":"relative/path","source_location":null,"weight":1.0}
  ],
  "hyperedges": [
    {"id":"snake_case_id","label":"Human Readable Label","nodes":["node_id1","node_id2","node_id3"],"relation":"participate_in|implement|form","confidence":"EXTRACTED|INFERRED","confidence_score":0.75,"source_file":"relative/path"}
  ],
  "input_tokens": 0,
  "output_tokens": 0
}
```

After writing the JSON file, your reply is exactly the path: `graphify-out/.graphify_chunk_CHUNK_PADDED.json`. No other text.
